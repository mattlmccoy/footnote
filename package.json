{
  "name": "footnote",
  "version": "0.1.0",
  "description": "Footnote — Google Docs for native-LaTeX writing and reviewing",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
