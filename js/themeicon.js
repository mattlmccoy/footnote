// The theme toggle shows the theme you'll switch TO: a sun while dark (tap for light), a moon while light.
// Pure so the icon can't drift from the actual theme. Both portals use it.
export function themeIconName(isDark) { return isDark ? 'ti-sun' : 'ti-moon'; }
